spring:
  datasource:
    url: jdbc:mysql://localhost:3306/umc9th?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root      # 너가 쓰는 아이디
    password: yubin1579$   # 너 비번
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 개발 중이라면 update or create
    properties:
      hibernate:
        format_sql: true
        show_sql: true